# Wan2GP Prompt Enhancer — minimal requirements
#
# PyTorch must be installed separately with the correct CUDA version:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
# (use cu121 or cu118 for older GPU drivers; setup.bat handles this automatically)

# Memory management / offloading
mmgp>=3.7.6

# HuggingFace / ML stack
transformers>=4.54.0
tokenizers>=0.20.3
accelerate>=1.1.1
diffusers>=0.36.0
safetensors
huggingface_hub

# Quantization backends
optimum[quanto]
gguf

# Vision utilities
einops
timm

# Video decoding
av

# UI + utilities
gradio>=5.29.0

# API server (for Flutter desktop client)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
Pillow
numpy
tqdm
xxhash
nvidia-ml-py 
misaki
stringzilla==4.0.14
xxhash
munch
wetext==0.1.2
# ── Optional acceleration (installed separately by setup.bat) ──
# triton-windows            # Windows; on Linux use: triton
# flash-attn                # Flash Attention 2 — ~5x faster vllm text generation
# sageattention             # Sage Attention 2  — ~40% faster diffusion
# llamacpp-gguf-cuda        # GGUF llama.cpp CUDA dequant kernels

# ── Other optional ──
# transformers==4.46.3  # for llamallava pre-patch
# rembg==2.0.65         # non-GPU fallback
# huggingface_hub[hf_xet]  # slows down everything
